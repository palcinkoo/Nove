{
  "name": "system-utility",
  "version": "3.1.0",
  "private": true,
  "description": "System Utility — secure data synchronization platform (server + dashboard + android)",
  "scripts": {
    "install:all": "npm --prefix dashboard install && npm --prefix server install",
    "dev": "npm --prefix dashboard run dev",
    "dev:full": "sh -c 'npm --prefix server run dev & npm --prefix dashboard run dev'",
    "build": "npm --prefix dashboard run build",
    "preview": "npm --prefix dashboard run preview",
    "start:server": "npm --prefix server run start"
  }
}
